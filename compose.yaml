services:
  feelsocialdb:
    container_name: feelsocialdb
    image: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: feelsocialdb
      POSTGRES_PASSWORD: admin
    ports:
      - "5434:5432"

  feelsocial_api:
    container_name: feelsocialapi
    image: feelsocial_api
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - feelsocialdb
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:admin@feelsocialdb:5434/feelsocialdb

volumes:
  pgdata: